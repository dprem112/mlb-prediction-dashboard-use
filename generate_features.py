# Placeholder feature generator for each matchup
# Normally you'd fetch real stats, but we return dummy features for now

def build_features(game):
    # Return mock feature values
    return [1.0, -0.5, 2, -1, 0.4]
